#include <iostream>
#include <string>
#include "Car.h"

int main(){

// When the constructor is called, it should create and add the following objects to the array of pointers:

// Tesla('S', 10000);
// Ford(1, 20000);
// Car(30000);
// Ford(2, 40000); 
// Tesla('T', 50000);

    return 0;
}
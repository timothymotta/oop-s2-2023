// Timothy Motta a1831126

// Given an array of integers, write a function to calculate the sum of the elements if it is a palindrome array. 
// If it is not a palindrome array, your function must return -2. 
// Your function must call separate functions to check whether or not the array is a palindrome and to calculate the sum of
// its elements.  If the length is 0 or negative each function must return -1 or false as its result.

#include <iostream>
usnig namespace std;

int palindrome_sum(int integers[], int length);
bool is_array_palindrome(int integers[], int length);
int sum_integers(int integers[], int length);

int main(){

    return 0;
}